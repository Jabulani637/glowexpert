# TODO

- [ ] Step 1: Harden admin re-entry by forcing backend `/api/admin/me` validation on every admin page load.
- [ ] Step 2: Confirm all admin entrypoints call `requireLogin()` (or add guards where missing).
- [ ] Step 3: Add backend/route hardening notes if needed.
- [ ] Step 4: Run backend tests (if available) and quick manual auth flow verification.

